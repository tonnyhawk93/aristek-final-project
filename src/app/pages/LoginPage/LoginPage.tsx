import React from "react";
import { LoginPageLayout, LoginForm } from "app/components";

const LoginPage = () => (
  <LoginPageLayout>
    <LoginForm />
  </LoginPageLayout>
);

export default LoginPage;
