query getAlbumFull($id: ID!) {
  album(id: $id) {
    id
    title
    user {
      id
      name
      username
    }
    photos {
      data {
        id
        title
        url
        thumbnailUrl
      }
    }
  }
}
